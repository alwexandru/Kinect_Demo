<Window x:Class="Kinect2.ColorStream.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Loaded="MainWindowLoaded"
        DataContext="{Binding RelativeSource={RelativeSource Self}}" 
        SizeToContent="WidthAndHeight">
    <StackPanel>
        <TextBlock Text="{Binding Message}" FontSize="30" HorizontalAlignment="Center"/>
        <Image Height="480" Width="640" Source="{Binding ImageSource}"/>
    </StackPanel>
</Window>
